# ---- Raw data (download from PhysioNet, never committed) ----
data/raw/
*.zip

# ---- Feature matrices (multi-GB) live in data/processed and the repo root; ----
# ---- they are regenerated by the notebooks, never committed. Scoped so the ----
# ---- tiny per-model feature-name lists under models/ ARE kept. ----
data/processed/*_features.csv
data/processed/*features*.csv
/*_features.csv
/*features*.csv
*_pool*.csv
*pool*.csv
discovery_families_mean.csv
clinical_intervals_features.csv

# ---- Caches and resumable checkpoints ----
*_cache*
*cache*
*.npz
*_ckpt.npz
*_ab_ckpt.npz
*_ckpt.pt
fold10_eval_cache.csv

# ---- Backups ----
backups/

# ---- Notebook checkpoints ----
.ipynb_checkpoints/

# ---- Python bytecode / build ----
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# ---- Virtual environments ----
venv/
.venv/
env/
.env

# ---- OS cruft ----
.DS_Store
Thumbs.db

# ---- stray test file (destination mount disallows delete) ----
.owtest

# ---- Large binaries handled by Git LFS if they ever exceed 100 MB ----
# (Currently every frozen model binary is < 100 MB and committed directly.)
# LFS-track these extensions if a file grows past 100 MB: *.joblib *.pt *.pth *.npy
# See models/LARGE_FILES.md.

# metrics-harmonization backups (not committed)
*.bak
*.bak_*
